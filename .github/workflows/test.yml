name: Run tests
on:
  push:
    branches: [ "master", "dev" ]
  pull_request:
    branches: [ "master", "dev" ]

jobs:
  build_and_test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup dotnet
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.x'

    - name: Restore dependencies
      run: dotnet restore

    - name: Tmp build
      run: |
        dotnet build --no-restore BookToAudio/BookToAudio.Core
        dotnet build --no-restore BookToAudio/BookToAudio.Realtime
        dotnet build --no-restore BookToAudio/BookToAudio.Web
        dotnet build --no-restore BookToAudio/BookToAudio.SeleniumTests
        dotnet build --no-restore BookToAudio/BookToAudio.Infra
        dotnet build --no-restore BookToAudio/BookToAudio.TestingInfra
        dotnet build --no-restore BookToAudio/BookToAudio.Api
        dotnet build --no-restore BookToAudio/BookToAudio.IntegrationTests
        dotnet build --no-restore BookToAudio/BookToAudio.UnitTests

    - name: Build
      run: dotnet build --no-restore

    - name: Run tests
      run: dotnet test --no-build --no-restore --verbosity normal